import axios from 'axios'
import history from '../history'

//Action types

//Action Creators

//Thunk Creators

//Initial State
const initialSate = {}

//Reducer

export default function(state = initialSate, action) {
  switch (action.type) {
    default:
      return state
  }
}
